import { h } from 'preact';
import phoneScreenshot from '../assets/rough-screenshot-white.png';

export default () => (
  <div class="phone-section">
    <img class="phone-screenshot" src={phoneScreenshot} />
  </div>
);
